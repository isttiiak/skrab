{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline permissions for the Skrab panel and the pinned-items widget. Deliberately narrow: the webview renders untrusted copied content, so it gets no filesystem, shell, or SQL access. Everything system-level goes through typed Rust commands.",
  "windows": ["main", "capture-overlay", "editor"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-start-dragging",
    "os:default",
    "process:default",
    "store:default",
    "opener:allow-open-url",
    "notification:default",
    "global-shortcut:default",
    "autostart:default",
    "core:window:allow-close"
  ]
}
